--- ../src-base/minecraft/net/minecraft/block/BlockCrops.java
+++ ../src-work/minecraft/net/minecraft/block/BlockCrops.java
@@ -82,6 +82,9 @@
                 if(net.minecraftforge.common.ForgeHooks.onCropsGrowPre(worldIn, pos, state, rand.nextInt((int)(25.0F / f) + 1) == 0))
                 {
                     worldIn.setBlockState(pos, this.withAge(i + 1), 2);
+                    //Lava-test - implement grow event in ForgeHooks
+                    //IBlockState data = this.withAge(i + 1);
+                    //org.bukkit.craftbukkit.event.CraftEventFactory.handleBlockGrowEvent(worldIn, pos.getX(), pos.getY(), pos.getZ(), this, getMetaFromState(data));
                     net.minecraftforge.common.ForgeHooks.onCropsGrowPost(worldIn, pos, state, worldIn.getBlockState(pos));
                 }
             }
@@ -97,8 +100,12 @@
         {
             i = j;
         }
-
-        worldIn.setBlockState(pos, this.withAge(i), 2);
+        
+        //Craftbukkit start - grow event
+        //worldIn.setBlockState(pos, this.withAge(i), 2);
+        IBlockState data = this.withAge(i);
+        org.bukkit.craftbukkit.event.CraftEventFactory.handleBlockGrowEvent(worldIn, pos.getX(), pos.getY(), pos.getZ(), this, getMetaFromState(data));
+        //Craftbukkit end
     }
 
     protected int getBonemealAgeIncrease(World worldIn)
