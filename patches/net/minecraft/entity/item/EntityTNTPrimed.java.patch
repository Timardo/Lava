--- ../src-base/minecraft/net/minecraft/entity/item/EntityTNTPrimed.java
+++ ../src-work/minecraft/net/minecraft/entity/item/EntityTNTPrimed.java
@@ -18,6 +18,9 @@
     private EntityLivingBase tntPlacedBy;
     private int fuse;
 
+    public float yield = 4; // CraftBukkit - add field TODO seems useless
+    public boolean isIncendiary = false; // CraftBukkit - add field
+
     public EntityTNTPrimed(World worldIn)
     {
         super(worldIn);
@@ -84,12 +87,13 @@
 
         if (this.fuse <= 0)
         {
-            this.setDead();
+            // this.setDead(); Craftbukkit - Need to reverse the order of the explosion and the entity death so we have a location for the event TODO check if this is true
 
             if (!this.world.isRemote)
             {
                 this.explode();
             }
+            this.setDead(); //Craftbukkit
         }
         else
         {
@@ -100,8 +104,17 @@
 
     private void explode()
     {
-        float f = 4.0F;
-        this.world.createExplosion(this, this.posX, this.posY + (double)(this.height / 16.0F), this.posZ, 4.0F, true);
+        // CraftBukkit start
+        //float f = 4.0F;
+        //this.world.createExplosion(this, this.posX, this.posY + (double)(this.height / 16.0F), this.posZ, 4.0F, true);
+        org.bukkit.craftbukkit.CraftServer server = this.world.getServer();
+        org.bukkit.event.entity.ExplosionPrimeEvent event = new org.bukkit.event.entity.ExplosionPrimeEvent((org.bukkit.entity.Explosive) org.bukkit.craftbukkit.entity.CraftEntity.getEntity(server, this));
+        server.getPluginManager().callEvent(event);
+
+        if (!event.isCancelled()) {
+            this.world.newExplosion(this, this.posX, this.posY + (double) (this.height / 16.0F), this.posZ, event.getRadius(), event.getFire(), true);
+        }
+        // CraftBukkit end
     }
 
     protected void writeEntityToNBT(NBTTagCompound compound)
