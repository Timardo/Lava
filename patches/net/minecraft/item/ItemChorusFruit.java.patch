--- ../src-base/minecraft/net/minecraft/item/ItemChorusFruit.java
+++ ../src-work/minecraft/net/minecraft/item/ItemChorusFruit.java
@@ -30,6 +30,18 @@
                 double d4 = MathHelper.clamp(entityLiving.posY + (double)(entityLiving.getRNG().nextInt(16) - 8), 0.0D, (double)(worldIn.getActualHeight() - 1));
                 double d5 = entityLiving.posZ + (entityLiving.getRNG().nextDouble() - 0.5D) * 16.0D;
 
+                //Craftbukkit start - event
+                if (entityLiving instanceof net.minecraft.entity.player.EntityPlayerMP) {
+                    org.bukkit.event.player.PlayerTeleportEvent teleEvent = new org.bukkit.event.player.PlayerTeleportEvent(((net.minecraft.entity.player.EntityPlayerMP) entityLiving).getBukkitEntity(), ((net.minecraft.entity.player.EntityPlayerMP) entityLiving).getBukkitEntity().getLocation(), new org.bukkit.Location(((net.minecraft.entity.player.EntityPlayerMP) entityLiving).getBukkitEntity().getWorld(), d3, d4, d5), org.bukkit.event.player.PlayerTeleportEvent.TeleportCause.CHORUS_FRUIT);
+                    worldIn.getServer().getPluginManager().callEvent(teleEvent);
+                    if (teleEvent.isCancelled()) {
+                        break;
+                    }
+                    d3 = teleEvent.getTo().getX();
+                    d4 = teleEvent.getTo().getY();
+                    d5 = teleEvent.getTo().getZ();
+                }
+                //Craftbukkit end
                 if (entityLiving.isRiding())
                 {
                     entityLiving.dismountRidingEntity();
